(ns blutwurst.planner-test
  (:require [clojure.test :refer :all]
            [blutwurst.planner :as foo]))

(deftest planner-tests
  (testing "Testing the process of sequencing tables."
    ;(let [simple-schema 
    ))
